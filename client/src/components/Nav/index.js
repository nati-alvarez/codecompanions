import React, {Component} from 'react';

class Nav extends Component {
    render(){
        return (
            <nav className="top-bar">
                <div className="top-bar-left menu">
                    <a className="logo" href="#">_CODE COMPANIONS</a>
                </div>
            </nav>
        );
    }
}

export default Nav;